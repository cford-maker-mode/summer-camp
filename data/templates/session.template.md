---
# Session Template
# Copy this file to /data/{year}/summer-{child}/sessions/session-{number}.md
# Replace all {placeholders} with actual values

id: session-{number}               # e.g., session-001
campId: camp-{slug}                # Must match a camp file ID
summerId: summer-{year}-{child}    # Must match the summer.md ID
startDate: {YYYY-MM-DD}
endDate: {YYYY-MM-DD}
status: planned                    # planned | confirmed | waitlisted
backupSessionId:                   # Optional: ID of backup session if waitlisted
signupTaskComplete: false          # true when signup is submitted
createdAt: {YYYY-MM-DD}
updatedAt: {YYYY-MM-DD}
---

## Notes

{Session-specific notes - confirmation number, special requests, etc.}
